# Aero Code Environment Configuration

# WebSocket Configuration (for Web Mode)
# URL for WebSocket server connection (optional, auto-detected if not set)
# VITE_WS_URL=ws://localhost:8765/ws

# WebSocket port (used when VITE_WS_URL is not set)
VITE_WS_PORT=8765

# Rust Backend Configuration
# Port for WebSocket server in Tauri backend
AERO_WS_PORT=8765
